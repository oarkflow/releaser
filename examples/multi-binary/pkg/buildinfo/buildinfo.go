// Package buildinfo contains build-time information
package buildinfo

var (
	// Version is the version of the application
	Version = "dev"
	// Commit is the git commit hash
	Commit = "none"
	// BuildDate is the build date
	BuildTime = "unknown"
)
